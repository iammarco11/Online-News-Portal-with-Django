{
    "builds": [{
        "src": "newsportal/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "newsportal/wsgi.py"
        }
    ]
}
